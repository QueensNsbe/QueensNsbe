{
  "name": "queensnsbe",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "tinacms build --skip-cloud-checks && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "astro:dev": "astro dev"
  },
  "dependencies": {
    "@tinacms/cli": "^2.6.0",
    "astro": "^7.2.0",
    "tinacms": "^3.12.0"
  }
}
